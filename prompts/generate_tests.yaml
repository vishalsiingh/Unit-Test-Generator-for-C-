instruction: |
  Given the following C++ source code, write Google Test-based unit tests:
  - Include the correct headers.
  - Use TEST or TEST_F macros.
  - Cover each public method with 2-3 unique test cases.
  - Avoid duplicate or trivial tests.
  - The output should be a complete, compilable C++ file.
